# Data Files

This directory contains JSON data files for the website (products, news, testimonials, etc.).
They are managed via the admin panel and synced to GitHub.

Default data is embedded in the app bundle; these JSON files are loaded when GitHub sync is enabled.
